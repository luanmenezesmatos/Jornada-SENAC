USE ideastudio;

# Sintaxe: UPDATE tabela SET nomecoluna = "valor que vai colocar" WHERE primária = numero;
UPDATE clientes SET emp_id = 1 WHERE cli_id = 3; # Nesse caso, iremos adicionar uma empresa específica (através do ID) à um cliente (referente à tabela de Clientes)